[project]
name = "stackscan"
version = "1.0.0"
description = "Fast tech stack fingerprinting CLI — like Wappalyzer but open source"
requires-python = ">=3.10"

[tool.poetry.dependencies]
httpx = ">=0.27"
rich = ">=13"
typer = ">=0.12"
dnspython = ">=2.6"
